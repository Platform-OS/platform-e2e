import { test } from '@playwright/test';

test.describe('OAuth authentications', () => {
  // Tests temporarily disabled — OAuth fixture setup needed
  // Page objects are in page-object.ts and oauth-page.ts
});
